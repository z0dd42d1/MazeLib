<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:MazeLib.UI.ViewModels;assembly=MazeLib.UI"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
        Background="DarkGray"
        x:Class="MazeLib.UI.Views.MainWindow"
        Icon="/Assets/avalonia-logo.ico"
        Title="MazeLib.UI"
        xmlns:local="clr-namespace:MazeLib.UI.Converters;assembly=MazeLib.UI">

  <Window.Resources>
    <local:StringToIntConverter x:Key="StringToIntConverter" />
  </Window.Resources>

  <Design.DataContext>
    <vm:MainWindowViewModel />
  </Design.DataContext>

  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="250" />
      <ColumnDefinition Width="*" />
    </Grid.ColumnDefinitions>
    <Grid.RowDefinitions>
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>
    <StackPanel Grid.Row="0" Grid.Column="0" Spacing="5">
      <Button
        Content="Generate maze"
        Command="{Binding DoTheThing}"
        HorizontalAlignment="Stretch" VerticalAlignment="Center" />
      <DropDown
        Grid.Row="0" Grid.Column="0"
        HorizontalAlignment="Stretch" VerticalAlignment="Center" />
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
          <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Grid.Column="0"
          Text="Width"
          HorizontalAlignment="Center" VerticalAlignment="Center" />
        <TextBox
          Text="{Binding Width, Converter={StaticResource StringToIntConverter}}"
        Grid.Row="0" Grid.Column="1" MinWidth="50" VerticalAlignment="Center" />
      </Grid>
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
          <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Grid.Column="0"
          Text="Height"
          HorizontalAlignment="Center" VerticalAlignment="Center" />
        <TextBox
          Text="{Binding Height, Converter={StaticResource StringToIntConverter}}"
          Grid.Row="0" Grid.Column="1" MinWidth="50" VerticalAlignment="Center" />
      </Grid>
    </StackPanel>
    <Canvas Height="400" Width="400"
    HorizontalAlignment="Center"
    VerticalAlignment="Center" />
  </Grid>
</Window>